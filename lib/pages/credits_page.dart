import 'package:flutter/material.dart';
import 'package:telalogin/widgets/record.dart';

class CreditsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Record());
  }
}
